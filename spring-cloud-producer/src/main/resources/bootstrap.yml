spring:
  application:
    name: producer
  cloud:
    config:
      fail-fast: true
      label: master
      discovery:
        enabled: true
        service-id: config

eureka:
  instance:
    hostname: producer
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/