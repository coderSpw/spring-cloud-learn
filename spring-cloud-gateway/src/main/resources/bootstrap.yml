spring:
  application:
    name: gateway
  cloud:
    config:
      #uri: http://localhost:6000
      fail-fast: true
      label: master
      discovery:
        enabled: true
        service-id: config